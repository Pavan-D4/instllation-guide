install java on aws linux

sudo yum install java-17-amazon-corretto-devel -y

ubuntu

sudo apt install openjdk-11-jdk -y

For older RHEL versions using yum, the command would be:
sudo yum install java-17-openjdk-devel
