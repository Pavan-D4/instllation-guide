apiVersion: v1
kind: Pod
metadata:
  name: my-app-pod
  labels:
    app: nginx
spec:
  containers:
  - name: my-app-container
    image: nginx:1.21.6
    ports:
    - containerPort: 80
    env:
    - name: ENVIRONMENT
      value: "production"
