apiVersion: v1
kind: Service
metadata:
  name: welcome-home-service
spec:
  type: NodePort
  selector:
    app: welcome-home
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 8080
      nodePort: 30004
